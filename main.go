// Main package for the lerning projects
// This will act as a launcher for the different created projects
package main

import (
	"SupUmbrela/core"
)

// === === === === === === === === ===
func main() {
	SupCore.Run()
}
